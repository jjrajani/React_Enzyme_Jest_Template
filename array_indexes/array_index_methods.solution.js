function findIndexOf(element, array) {
    return array.indexOf(element);
}

function findLastIndexOf(element, array) {
    return array.lastIndexOf(element);
}

const guessTheIndex = 2;
const guessTheLastIndex = 3;

export { findIndexOf, findLastIndexOf, guessTheIndex, guessTheLastIndex };
